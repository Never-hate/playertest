PORT=3000
# Comma-separated list of allowed domains (use * for all, though not recommended for production)
ALLOWED_DOMAINS=*
# Headers to inject into upstream requests
CUSTOM_REFERER=https://example.com/
CUSTOM_ORIGIN=https://example.com
CUSTOM_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64)
